import React from "react";
import { Footer as MantineFooter } from "@mantine/core";

export default function Footer() {
  return (
    <MantineFooter height={60} p="md">
      Application MantineFooter
    </MantineFooter>
  );
}
